# 0.5.0

- Added `TeleportTarget.CODEC` for persisting teleport targets.

# 0.4.0

- Added `ItemStackData` for cross-version custom item data access.
- Added `SyrupProfiles` for player profile lookup and caching.

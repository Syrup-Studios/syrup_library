package net.syrupstudios.syruplibrary.config;

/** Controls whether a configuration value appears and can be changed in the local editor. */
public enum ConfigEditPolicy {
    EDITABLE,
    READ_ONLY,
    HIDDEN
}
